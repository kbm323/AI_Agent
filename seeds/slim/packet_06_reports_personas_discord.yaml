goal: Implement presentation/orchestration edge behavior as pure tested modules; no
  live Discord calls.
task_type: code
constraints:
- Do not recreate implemented ACs. Inspect matching src/ and tests/ first.
- If implementation and tests already exist, run tests and mark verified instead of
  editing.
- 'Use strict TDD for any missing behavior: failing test first, then minimal implementation.'
- All Coordinator-Worker contracts use filesystem-based JSON I/O.
- No Hermes Core modifications.
- 'Keep token use low: read only target files; do not load full original seed unless
  necessary.'
- 'Rate-limit safe: no live LLM calls in tests; mock or pure-function only.'
- 'Target packet only: packet_06_reports_personas_discord. Do not implement other
  packets.'
acceptance_criteria:
- AC22 Periodic summaries and self-reflection reports
- AC23 agent.yaml + persona.md role specs with Git versioning
- AC24 6-7 persistent team-leader Discord bots; internal specialists as workers only
- AC25 Original Discord thread delivery and result/briefing cross-post
- AC26 Same-thread follow-up extends existing meeting_id
target_files:
- src/periodic_summary.py
- src/persona_spec_loader.py
- src/team_leader_registry.py
- src/discord_delivery.py
- src/followup_thread_router.py
- tests/test_periodic_summary.py
- tests/test_persona_spec_loader.py
- tests/test_team_leader_registry.py
- tests/test_discord_delivery.py
- tests/test_followup_thread_router.py
verification_commands:
- pytest tests/test_periodic_summary.py tests/test_persona_spec_loader.py tests/test_team_leader_registry.py
  tests/test_discord_delivery.py tests/test_followup_thread_router.py -q
evaluation_principles:
- name: no_duplicate_work
  weight: 0.3
  description: Existing implementation is verified before creating new files
- name: targeted_scope
  weight: 0.25
  description: Only packet target files/ACs are touched
- name: test_evidence
  weight: 0.25
  description: Behavior is proven by focused tests
- name: seed_alignment
  weight: 0.2
  description: Behavior satisfies parent remaining seed constraints
exit_conditions:
- name: packet_verified
  description: All packet verification commands pass
  criteria: state transitions to completed
- name: already_complete
  description: Existing code/tests already satisfy packet
  criteria: no edits; evidence recorded
- name: blocked
  description: Missing prerequisite outside packet scope
  criteria: stop and report blocker without broadening scope
metadata:
  seed_id: packet_06_reports_personas_discord
  title: Reports, persona specs, Discord delivery/follow-up
  parent_seed_id: seed_08605bf07c22_remaining
  created_at: '2026-06-16T14:19:55Z'
  generation_mode: manual_slim_split
  token_strategy: targeted-files-only; verify-before-edit; no full-seed rerun
